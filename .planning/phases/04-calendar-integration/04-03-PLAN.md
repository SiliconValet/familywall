---
phase: 04-calendar-integration
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - client/src/components/calendar/CalendarView.tsx
  - client/src/components/calendar/DailyAgenda.tsx
  - client/src/components/calendar/WeeklyAgenda.tsx
  - client/src/components/calendar/MonthlyGrid.tsx
  - client/src/components/calendar/EventCard.tsx
  - client/src/components/calendar/TimeIndicator.tsx
autonomous: true
requirements: [CAL-04, CAL-05, CAL-06, CAL-07, CAL-08, CAL-10, CAL-11, CAL-12]

must_haves:
  truths:
    - "User can switch between Daily, Weekly, and Monthly views via toggle group"
    - "User can navigate previous/next period with arrow buttons"
    - "User can jump to today with a single tap"
    - "Daily view shows vertical agenda with all-day section at top and time-sorted events below"
    - "Weekly view shows 7-column layout (Mon-Sun) with abbreviated event cards"
    - "Monthly view shows calendar grid with color-coded event dots"
    - "Tapping event card expands to show full details inline"
    - "Tapping monthly grid cell navigates to daily view for that date"
    - "Current time indicator appears as colored line in daily view"
    - "All-day events display in separate section from timed events"
  artifacts:
    - path: "client/src/components/calendar/CalendarView.tsx"
      provides: "Main calendar container with view switcher, navigation, and refresh"
      min_lines: 80
    - path: "client/src/components/calendar/DailyAgenda.tsx"
      provides: "Vertical agenda list with all-day section and time indicator"
      min_lines: 60
    - path: "client/src/components/calendar/WeeklyAgenda.tsx"
      provides: "7-column weekly agenda layout"
      min_lines: 50
    - path: "client/src/components/calendar/MonthlyGrid.tsx"
      provides: "Calendar grid with event dots and cell tap navigation"
      min_lines: 60
    - path: "client/src/components/calendar/EventCard.tsx"
      provides: "Expandable event card with color bar and inline details"
      min_lines: 40
    - path: "client/src/components/calendar/TimeIndicator.tsx"
      provides: "Current time line with label for daily view"
      min_lines: 20
  key_links:
    - from: "client/src/components/calendar/CalendarView.tsx"
      to: "client/src/hooks/useCalendarData.ts"
      via: "import useCalendarData"
      pattern: "useCalendarData"
    - from: "client/src/components/calendar/CalendarView.tsx"
      to: "client/src/hooks/useCalendarSync.ts"
      via: "import useCalendarSync"
      pattern: "useCalendarSync"
    - from: "client/src/components/calendar/DailyAgenda.tsx"
      to: "client/src/components/calendar/EventCard.tsx"
      via: "renders EventCard for each item"
      pattern: "<EventCard"
    - from: "client/src/components/calendar/MonthlyGrid.tsx"
      to: "CalendarView.tsx"
      via: "calls onDayClick to navigate to daily view"
      pattern: "onDayClick"
---

<objective>
Build the calendar view UI components: main container with navigation/view switching, three view modes (daily agenda, weekly multi-column, monthly grid), expandable event cards, and current time indicator.

Purpose: Delivers the core user-facing calendar experience with all three view modes, navigation, and event interaction. These components consume the types and hooks from Plan 02.

Output: Complete set of calendar view components ready to be wired into App.tsx (done in Plan 04).
</objective>

<execution_context>
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calendar-integration/04-CONTEXT.md
@.planning/phases/04-calendar-integration/04-UI-SPEC.md
@.planning/phases/04-calendar-integration/04-02-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 02 that these components consume -->

From client/src/types/calendar.ts:
```typescript
export type CalendarViewMode = 'daily' | 'weekly' | 'monthly';

export interface CalendarEvent {
  id: string;
  summary: string | null;
  description: string | null;
  location: string | null;
  startTime: string | null;
  endTime: string | null;
  startDate: string | null;
  endDate: string | null;
  isAllDay: boolean;
  familyMemberId: number | null;
  familyMemberName: string | null;
  calendarName: string;
}

export interface MergedTimelineItem {
  type: 'event' | 'chore';
  sortTime: number;
  isAllDay: boolean;
  data: CalendarEvent | Chore;
}
```

From client/src/hooks/useCalendarData.ts:
```typescript
export function useCalendarData(viewDate: Date, viewMode: 'daily' | 'weekly' | 'monthly') {
  return {
    mergedItems, allDayItems, timedItems, loading, error, authStatus, refetch
  };
}
```

From client/src/hooks/useCalendarSync.ts:
```typescript
export function useCalendarSync() {
  return { syncing, lastSynced, syncError, triggerSync };
}
```

From client/src/types/chore.ts:
```typescript
export interface Chore {
  id: number;
  title: string;
  assigned_to: number;
  status: 'active' | 'completed' | 'auto_completed';
  points: number;
  assignee_name: string;
  // ... other fields
}
```

Existing shadcn components (all installed):
- Button: `import { Button } from '../ui/button'`
- Card: `import { Card } from '../ui/card'`
- ToggleGroup: `import { ToggleGroup, ToggleGroupItem } from '../ui/toggle-group'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventCard and TimeIndicator components</name>
  <files>client/src/components/calendar/EventCard.tsx, client/src/components/calendar/TimeIndicator.tsx</files>
  <read_first>
    - client/src/components/ChoreCard.tsx (existing card pattern with left border accent, 48px touch targets)
    - client/src/types/calendar.ts (CalendarEvent, MergedTimelineItem types from Plan 02)
    - client/src/types/chore.ts (Chore interface for rendering chores in calendar)
    - .planning/phases/04-calendar-integration/04-UI-SPEC.md (EventCard layout, TimeIndicator specs)
    - .planning/phases/04-calendar-integration/04-CONTEXT.md (D-18 card content, D-19 tap expansion, D-21 color-coding, D-34 chore checkbox)
    - client/src/components/ui/card.tsx (shadcn Card component)
    - client/src/components/ui/checkbox.tsx (shadcn Checkbox for chore completion in calendar)
    - client/src/index.css (existing CSS custom properties including --chart-1 through --chart-4)
  </read_first>
  <action>
1. Create `client/src/components/calendar/EventCard.tsx`:
   - Props: `item: MergedTimelineItem`, `expanded: boolean`, `onToggleExpand: (id: string) => void`, `onCompleteChore?: (id: number) => void`
   - **Click handler (CAL-10, D-19):** Card wrapper `<div>` has `onClick={() => onToggleExpand(item.type === 'event' ? (item.data as CalendarEvent).id : String((item.data as Chore).id))}` to trigger expansion on tap. For chore items, the checkbox click must call `e.stopPropagation()` to prevent expansion when completing a chore.
   - Collapsed state (48px min height per D-30): Shows time + title only with 4px left border in family member color (per D-18, D-21)
   - For events: Time label "2:00 PM" (14px text-sm muted), title (18px text-lg) per D-18
   - For chores (type === 'chore'): Checkbox (48px, per D-34) + title + points badge, same styling as ChoreCard
   - Left border color: Use CSS variable `--chart-{N}` mapped from `familyMemberId`. Map member IDs to chart colors cyclically: `const chartColorIndex = ((item.data.familyMemberId || 0) % 4) + 1; style={{ borderLeftColor: \`oklch(var(--chart-${chartColorIndex}))\` }}`
   - Expanded state (per D-19): Transition height auto with 300ms ease-out. Show:
     - Time range: "2:00 PM - 3:00 PM" (14px text-sm)
     - Location: "Location: [location]" (16px body) if present, hidden if null
     - Description: "[description]" (16px body) or "No description" if empty
   - Only one card expanded at a time: CalendarView manages expanded state, passes expanded boolean
   - Touch feedback: `active:scale-[0.96] transition-transform duration-150`
   - Completed chores: opacity-70 per D-35
   - Card padding: p-4 (16px) per UI-SPEC

   Format event times using date-fns:
   ```typescript
   import { format } from 'date-fns';
   const timeLabel = item.type === 'event' && !item.isAllDay && (item.data as CalendarEvent).startTime
     ? format(new Date((item.data as CalendarEvent).startTime!), 'h:mm a')
     : null;
   ```

2. Create `client/src/components/calendar/TimeIndicator.tsx`:
   - No props (self-contained, reads current time)
   - Renders: 4px height horizontal line in primary accent color `oklch(var(--primary))` per UI-SPEC
   - Time label: "10:23 AM" format (14px text-sm, primary accent color) per D-12, CAL-12
   - Updates every minute via `useEffect` + `setInterval(60000)`
   - Only renders if current time is between 6:00 AM and 10:00 PM (per D-12 timeline range)
   - Layout: `flex items-center gap-2` with line as `flex-1 h-1 bg-primary` and label on right
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest && npx tsc --noEmit client/src/components/calendar/EventCard.tsx client/src/components/calendar/TimeIndicator.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/calendar/EventCard.tsx contains `export` and `EventCard`
    - client/src/components/calendar/EventCard.tsx contains `MergedTimelineItem`
    - client/src/components/calendar/EventCard.tsx contains `expanded`
    - client/src/components/calendar/EventCard.tsx contains `onToggleExpand`
    - client/src/components/calendar/EventCard.tsx contains `border-l-4` or `borderLeft`
    - client/src/components/calendar/EventCard.tsx contains `min-h-12` or `min-h-\[48px\]`
    - client/src/components/calendar/EventCard.tsx contains `300ms` or `duration-300`
    - client/src/components/calendar/EventCard.tsx contains `opacity` (for completed chore fade)
    - client/src/components/calendar/TimeIndicator.tsx contains `export` and `TimeIndicator`
    - client/src/components/calendar/TimeIndicator.tsx contains `setInterval` or `useEffect`
    - client/src/components/calendar/TimeIndicator.tsx contains `h:mm a` or `format`
    - client/src/components/calendar/TimeIndicator.tsx contains `bg-primary` or `--primary`
  </acceptance_criteria>
  <done>EventCard renders collapsed/expanded states with color bar, touch feedback, and chore completion support. TimeIndicator shows current time line that updates every minute.</done>
</task>

<task type="auto">
  <name>Task 2: CalendarView container and three view mode components</name>
  <files>client/src/components/calendar/CalendarView.tsx, client/src/components/calendar/DailyAgenda.tsx, client/src/components/calendar/WeeklyAgenda.tsx, client/src/components/calendar/MonthlyGrid.tsx</files>
  <read_first>
    - client/src/components/calendar/EventCard.tsx (created in Task 1, props interface)
    - client/src/components/calendar/TimeIndicator.tsx (created in Task 1)
    - client/src/hooks/useCalendarData.ts (useCalendarData return shape)
    - client/src/hooks/useCalendarSync.ts (useCalendarSync return shape)
    - client/src/hooks/useChoreData.ts (completeChore function for calendar view per D-34)
    - client/src/components/ChoreList.tsx (existing view toggle pattern)
    - client/src/components/ui/toggle-group.tsx (ToggleGroup for view switcher)
    - client/src/components/ui/button.tsx (Button for navigation)
    - .planning/phases/04-calendar-integration/04-UI-SPEC.md (all layout patterns, spacing, copy)
    - .planning/phases/04-calendar-integration/04-CONTEXT.md (D-11 through D-30, D-31 through D-35)
  </read_first>
  <action>
1. Create `client/src/components/calendar/CalendarView.tsx` — Main container per D-23 through D-29:
   - State: `viewMode: CalendarViewMode` (default 'daily'), `viewDate: Date` (default today), `expandedEventId: string | null`
   - Use `useCalendarData(viewDate, viewMode)` and `useCalendarSync()`
   - Use `useChoreData()` for `completeChore` function (per D-34)
   - Two-row top bar layout per D-23:
     - Header row: "Calendar" heading (24px text-2xl font-serif) + refresh icon button (48x48px, top-right) + settings gear icon (48x48px) per D-29
       - Refresh button: When `syncing`, show spinner (animated `animate-spin`). When not syncing, show refresh icon. Use hugeicons icon or a simple SVG refresh icon.
       - Settings gear: `onClick` triggers `onOpenSettings` callback prop (settings modal managed by parent or Plan 04)
     - Row 1: ToggleGroup with three items: "Daily", "Weekly", "Monthly" per D-24
       - Each toggle: `min-h-12` (48px), `px-6` padding
       - Active: primary accent background, white text
       - onChange: `setViewMode(value as CalendarViewMode)`
     - Row 2: Navigation per D-25, D-26, D-27, D-28
       - Layout: `flex items-center justify-between`
       - Left arrow button: 48x48px, `onClick` navigates previous period
       - Period label: 24px text-2xl, centered. Format per D-27:
         - Daily: `format(viewDate, 'MMMM d, yyyy')` => "March 23, 2026"
         - Weekly: `format(weekStart, 'MMM d') + '–' + format(weekEnd, 'd, yyyy')` => "Mar 17-23, 2026"
         - Monthly: `format(viewDate, 'MMMM yyyy')` => "March 2026"
       - Right arrow button: 48x48px
       - Today button: 48px height, text "Today" per D-28. Highlighted when viewing current period.
       - Navigation handlers using date-fns:
         - Previous: `addDays(viewDate, -1)` / `addWeeks(viewDate, -1)` / `addMonths(viewDate, -1)`
         - Next: `addDays(viewDate, 1)` / `addWeeks(viewDate, 1)` / `addMonths(viewDate, 1)`
         - Today: `setViewDate(new Date())`
   - Content area: Conditionally render DailyAgenda, WeeklyAgenda, or MonthlyGrid
   - Pass `expandedEventId` and `setExpandedEventId` to view components (only one event expanded at a time per UI-SPEC)
   - If `authStatus.connected === false`: Show empty state "No Calendar Connected" / "Connect your Google Calendar in Settings to see events here." per UI-SPEC copywriting
   - If `loading`: Show "Syncing calendar..." with spinner per UI-SPEC
   - SyncStatusBadge: If `lastSynced` and sync age > 15 min, show "Last synced X minutes ago" per UI-SPEC
   - Spacing per UI-SPEC: Header to Row 1: 16px (space-4), Row 1 to Row 2: 12px (space-3), Row 2 to content: 24px (space-6)
   - Export: `export function CalendarView({ onOpenSettings }: { onOpenSettings: () => void })`

2. Create `client/src/components/calendar/DailyAgenda.tsx` — Vertical agenda per D-12, D-17:
   - Props: `allDayItems: MergedTimelineItem[]`, `timedItems: MergedTimelineItem[]`, `expandedEventId: string | null`, `onToggleExpand: (id: string) => void`, `onCompleteChore: (id: number) => void`
   - All-Day Section (top, per D-17, CAL-11):
     - Heading: "All-Day Events" (14px text-sm text-muted-foreground)
     - Render EventCard for each allDayItems item
     - Hidden when no all-day events (collapsible)
     - Divider: 1px border-border below section
   - Timed Events Section:
     - Group events by hour, show time label "9:00 AM" (14px text-sm text-muted-foreground) before each group
     - Render EventCard for each timedItems item
     - Insert TimeIndicator component at the correct position (between the appropriate time groups based on current time)
   - Empty state: "No Events Today" heading / "Your schedule is clear. New events will sync automatically." body per UI-SPEC
   - Spacing per UI-SPEC: All-day to divider 16px, divider to first time 16px, time to card 8px, card to next time 16px, page margins px-6

3. Create `client/src/components/calendar/WeeklyAgenda.tsx` — 7-column layout per D-13:
   - Props: `items: MergedTimelineItem[]`, `viewDate: Date`, `expandedEventId: string | null`, `onToggleExpand: (id: string) => void`, `onCompleteChore: (id: number) => void`
   - Layout: CSS grid with 7 equal columns `grid grid-cols-7`
   - Column headers: "Mon 17", "Tue 18", etc. (14px text-sm font-semibold, centered, muted background)
     - Calculate dates using `startOfWeek(viewDate, { weekStartsOn: 1 })` and `addDays`
     - Format: `format(date, 'EEE d')` => "Mon 17"
   - Each column: Filter mergedItems for that day, render abbreviated EventCards
     - Abbreviated: Time + first 3 chars of title + color bar (space-constrained per UI-SPEC)
   - All-day events in each column: Show at top with "All-day:" label
   - Column padding: p-2 (8px), card gap: gap-2 (8px)
   - Empty state: "No Events This Week" / "You're free for [date range]. Events will appear when scheduled." per UI-SPEC
   - Scrolling: `overflow-y-auto` for each column if content exceeds viewport

4. Create `client/src/components/calendar/MonthlyGrid.tsx` — Calendar grid per D-14, D-15, D-16:
   - Props: `items: MergedTimelineItem[]`, `viewDate: Date`, `onDayClick: (date: Date) => void`
   - Layout: CSS grid with 7 columns `grid grid-cols-7`
   - Day headers: "Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat" (14px text-sm font-semibold, centered, 32px height)
   - Grid cells:
     - Height: 64px (`h-16`) per UI-SPEC exception
     - Border: 1px border-border
     - Padding: p-1 (4px)
     - Date number: 14px text-sm, top-left. Current month = foreground color, prev/next month = text-muted-foreground
     - Event dots: 8px diameter circles (`w-2 h-2 rounded-full`), family member chart color, max 3 visible per D-15
     - Overflow: "+N" label (14px text-sm) if more than 3 events per day
     - Dots layout: `flex gap-1 justify-center` below date number
   - Calculate grid: Use `startOfMonth`, `endOfMonth`, `startOfWeek`, `endOfWeek` from date-fns to get 35 or 42 day grid
   - Cell interaction per D-16:
     - `onClick={() => onDayClick(cellDate)}`
     - Touch feedback: `active:scale-[0.98] transition-transform duration-150`
   - No empty state for monthly view (grid always shows all days per UI-SPEC)
   - Color mapping for dots: Same `--chart-{N}` pattern as EventCard, use `style={{ backgroundColor: \`oklch(var(--chart-${colorIndex}))\` }}`
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest && npx tsc --noEmit client/src/components/calendar/CalendarView.tsx client/src/components/calendar/DailyAgenda.tsx client/src/components/calendar/WeeklyAgenda.tsx client/src/components/calendar/MonthlyGrid.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/calendar/CalendarView.tsx contains `export` and `CalendarView`
    - client/src/components/calendar/CalendarView.tsx contains `ToggleGroup` (view switcher)
    - client/src/components/calendar/CalendarView.tsx contains `addDays` or `addWeeks` or `addMonths` (navigation)
    - client/src/components/calendar/CalendarView.tsx contains `"Today"` (today button)
    - client/src/components/calendar/CalendarView.tsx contains `"No Calendar Connected"` (empty state)
    - client/src/components/calendar/CalendarView.tsx contains `useCalendarData`
    - client/src/components/calendar/CalendarView.tsx contains `useCalendarSync`
    - client/src/components/calendar/DailyAgenda.tsx contains `"All-Day Events"` or `All-Day`
    - client/src/components/calendar/DailyAgenda.tsx contains `TimeIndicator`
    - client/src/components/calendar/DailyAgenda.tsx contains `"No Events Today"` (empty state)
    - client/src/components/calendar/WeeklyAgenda.tsx contains `grid-cols-7`
    - client/src/components/calendar/WeeklyAgenda.tsx contains `weekStartsOn: 1` or `weekStartsOn`
    - client/src/components/calendar/MonthlyGrid.tsx contains `grid-cols-7`
    - client/src/components/calendar/MonthlyGrid.tsx contains `onDayClick`
    - client/src/components/calendar/MonthlyGrid.tsx contains `w-2 h-2` or `8px` (event dots)
    - client/src/components/calendar/MonthlyGrid.tsx contains `h-16` or `64px` (cell height)
  </acceptance_criteria>
  <done>Four calendar view components created: CalendarView (container with navigation and view switching), DailyAgenda (vertical timeline with all-day section and time indicator), WeeklyAgenda (7-column grid), MonthlyGrid (calendar grid with event dots and day-click navigation).</done>
</task>

</tasks>

<verification>
- All 6 calendar component files compile: `npx tsc --noEmit client/src/components/calendar/*.tsx`
- CalendarView imports and uses all three view components
- DailyAgenda renders EventCard and TimeIndicator
- MonthlyGrid handles onDayClick for daily navigation
</verification>

<success_criteria>
- Three calendar views render correctly with proper layouts
- Navigation between periods works (prev/next/today)
- View switching between Daily/Weekly/Monthly works
- Event cards expand/collapse with animation
- Monthly grid shows color-coded dots and supports cell tapping
- Current time indicator shows in daily view
- Empty states display when no events or not connected
</success_criteria>

<output>
After completion, create `.planning/phases/04-calendar-integration/04-03-SUMMARY.md`
</output>
