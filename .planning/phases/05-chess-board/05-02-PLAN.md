---
phase: 05-chess-board
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - client/src/components/chess/ChessBoard.tsx
  - client/src/components/chess/ChessSquare.tsx
  - client/src/components/chess/ChessSidebar.tsx
  - client/src/components/chess/MoveHistory.tsx
  - client/src/components/chess/PlayerBadge.tsx
autonomous: true
requirements: [CHESS-03, CHESS-04, CHESS-05, CHESS-06, CHESS-07, CHESS-08]

must_haves:
  truths:
    - "Board renders 8x8 grid of alternating light/dark gray squares with Unicode chess pieces"
    - "Tapping a piece highlights its square with blue border; tapping destination moves it"
    - "Tapping selected square again deselects it"
    - "Move history shows vertical list in format '1. e4', '1... e5' and auto-scrolls to latest"
    - "Sidebar shows player badges with turn indicator and New Game / Undo buttons"
  artifacts:
    - path: "client/src/components/chess/ChessBoard.tsx"
      provides: "8x8 CSS Grid board with tap-tap interaction"
      min_lines: 60
    - path: "client/src/components/chess/ChessSquare.tsx"
      provides: "Individual square with piece rendering and click handler"
      min_lines: 30
    - path: "client/src/components/chess/ChessSidebar.tsx"
      provides: "Sidebar container with player badges, controls, and move history"
      min_lines: 40
    - path: "client/src/components/chess/MoveHistory.tsx"
      provides: "Scrollable vertical move list with auto-scroll"
      min_lines: 30
    - path: "client/src/components/chess/PlayerBadge.tsx"
      provides: "Tappable player indicator with turn highlight"
      min_lines: 20
  key_links:
    - from: "client/src/components/chess/ChessBoard.tsx"
      to: "client/src/components/chess/ChessSquare.tsx"
      via: "renders 64 ChessSquare components"
      pattern: "ChessSquare"
    - from: "client/src/components/chess/ChessSidebar.tsx"
      to: "client/src/components/chess/MoveHistory.tsx"
      via: "renders MoveHistory in sidebar"
      pattern: "MoveHistory"
    - from: "client/src/components/chess/ChessSidebar.tsx"
      to: "client/src/components/chess/PlayerBadge.tsx"
      via: "renders two PlayerBadge components"
      pattern: "PlayerBadge"
---

<objective>
Build all chess UI components: the 8x8 board with tap-tap interaction, individual squares with Unicode pieces, sidebar with player badges, game controls, and scrollable move history.

Purpose: Create the visual chess experience following all locked design decisions (D-01 through D-17). These components consume the useChessGame hook from Plan 01.

Output: Five React components that together render a playable chess board with sidebar controls.
</objective>

<execution_context>
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-chess-board/05-CONTEXT.md
@.planning/phases/05-chess-board/05-RESEARCH.md
@.planning/phases/05-chess-board/05-UI-SPEC.md
@.planning/phases/05-chess-board/05-01-SUMMARY.md

<interfaces>
From client/src/hooks/useChessGame.ts (created in Plan 01):
```typescript
export function useChessGame(): {
  fen: string;
  chess: Chess;          // chess.js instance for board() method
  moves: string[];       // SAN move history array
  whitePlayerId: number | null;
  blackPlayerId: number | null;
  loading: boolean;
  makeMove: (from: string, to: string) => Promise<boolean>;
  undoMove: () => Promise<void>;
  resetGame: () => Promise<void>;
  setPlayer: (color: 'white' | 'black', playerId: number | null) => Promise<void>;
}
```

From client/src/types/chess.ts (created in Plan 01):
```typescript
export interface ChessGame { fen: string; moves: string[]; white_player_id: number | null; black_player_id: number | null; }
export type PieceColor = 'w' | 'b';
export interface SquareInfo { type: string; color: PieceColor; }
```

From client/src/components/ui/button.tsx (existing):
```typescript
export const Button: React.FC<ButtonProps>  // shadcn Button with variant prop
```

From client/src/hooks/useFamilyData.ts (existing):
```typescript
export function useFamilyData(): { members: FamilyMember[]; loading: boolean; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChessSquare and ChessBoard components</name>
  <files>client/src/components/chess/ChessSquare.tsx, client/src/components/chess/ChessBoard.tsx</files>
  <read_first>
    - client/src/hooks/useChessGame.ts (hook API to consume)
    - client/src/types/chess.ts (type definitions)
    - .planning/phases/05-chess-board/05-UI-SPEC.md (Visual Specification section for CSS, colors, layout)
    - .planning/phases/05-chess-board/05-RESEARCH.md (Pattern 2 tap-tap, Pitfall 5 aspect ratio, Pitfall 6 turn validation)
  </read_first>
  <action>
1. Create client/src/components/chess/ChessSquare.tsx:
   - Props: `{ piece: SquareInfo | null; isLight: boolean; isSelected: boolean; onClick: () => void; coordinate: string }`
   - Unicode piece map (per D-05, D-07):
     ```typescript
     const PIECE_SYMBOLS: Record<string, Record<string, string>> = {
       w: { p: '\u2659', n: '\u2658', b: '\u2657', r: '\u2656', q: '\u2655', k: '\u2654' },
       b: { p: '\u265F', n: '\u265E', b: '\u265D', r: '\u265C', q: '\u265B', k: '\u265A' }
     };
     ```
   - Render a div with onClick handler
   - CSS classes using Tailwind:
     - Base: `flex items-center justify-center cursor-pointer` with min-h-[44px] aspect-square
     - Light square (per D-06): `bg-[hsl(0,0%,85%)]`
     - Dark square (per D-06): `bg-[hsl(0,0%,60%)]`
     - Selected (per D-02): `border-4 border-[hsl(220,90%,50%)] box-border`
     - Font: `text-[clamp(2rem,8vw,3.5rem)]` with `font-['Segoe_UI_Symbol','Noto_Sans_Symbols','Apple_Color_Emoji',sans-serif]`
   - Display piece symbol from PIECE_SYMBOLS map if piece is not null, otherwise empty string

2. Create client/src/components/chess/ChessBoard.tsx:
   - Props: `{ chess: Chess; selectedSquare: string | null; onSquareClick: (square: string) => void }`
   - Use chess.board() to get 8x8 array of piece positions
   - Render CSS Grid container with Tailwind: `grid grid-cols-8 grid-rows-8 aspect-square max-w-[min(80vh,600px)] mx-auto`
   - Iterate rows 0-7 (rank 8 to 1, top to bottom) and columns 0-7 (file a to h):
     - Compute square name: `const file = 'abcdefgh'[col]; const square = file + (8 - row);`
     - Determine light/dark: `(row + col) % 2 === 0` is light
     - Render ChessSquare with piece from board array, isLight, isSelected (square === selectedSquare), onClick calling onSquareClick(square)
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/chess/ChessSquare.tsx exists and exports ChessSquare component
    - ChessSquare.tsx contains PIECE_SYMBOLS map with Unicode characters \u2654 through \u265F
    - ChessSquare.tsx contains bg-[hsl(0,0%,85%)] for light squares
    - ChessSquare.tsx contains bg-[hsl(0,0%,60%)] for dark squares
    - ChessSquare.tsx contains border-4 and border-[hsl(220,90%,50%)] for selected state
    - ChessSquare.tsx contains min-h-[44px] for touch target compliance
    - ChessSquare.tsx contains onClick prop handler
    - client/src/components/chess/ChessBoard.tsx exists and exports ChessBoard component
    - ChessBoard.tsx contains grid grid-cols-8
    - ChessBoard.tsx contains aspect-square
    - ChessBoard.tsx contains max-w-[min(80vh,600px)]
    - ChessBoard.tsx calls chess.board() to get piece positions
    - ChessBoard.tsx renders 64 ChessSquare components
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>ChessSquare renders individual squares with Unicode pieces and selection highlight. ChessBoard renders 8x8 CSS Grid with all squares and passes click events up.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlayerBadge, MoveHistory, and ChessSidebar components</name>
  <files>client/src/components/chess/PlayerBadge.tsx, client/src/components/chess/MoveHistory.tsx, client/src/components/chess/ChessSidebar.tsx</files>
  <read_first>
    - client/src/components/FamilyMemberBadge.tsx (badge pattern to follow)
    - client/src/components/ui/button.tsx (button usage pattern)
    - client/src/hooks/useFamilyData.ts (to get family member names)
    - .planning/phases/05-chess-board/05-UI-SPEC.md (Layout, Copywriting Contract, Player Badges, Move History Container sections)
    - .planning/phases/05-chess-board/05-RESEARCH.md (Pattern 4 auto-scroll)
  </read_first>
  <action>
1. Create client/src/components/chess/PlayerBadge.tsx:
   - Props: `{ color: 'white' | 'black'; playerName: string | null; isCurrentTurn: boolean; onClick: () => void }`
   - Render tappable div with min-h-[48px] for touch target (per D-11)
   - Display label: `White: ${playerName || 'Not Selected'}` or `Black: ${playerName || 'Not Selected'}` (per UI-SPEC copywriting)
   - Active turn styling (per D-08, UI-SPEC): add ring-2 ring-[hsl(220,90%,50%)] when isCurrentTurn is true
   - Chess piece icon: show white king (U+2654) for white badge, black king (U+265A) for black badge
   - Tailwind classes: `flex items-center gap-2 p-2 rounded-lg cursor-pointer border w-full`
   - Active press feedback: `active:scale-[0.96] transition-transform`

2. Create client/src/components/chess/MoveHistory.tsx:
   - Props: `{ moves: string[] }`
   - Format moves per D-15: `index % 2 === 0 ? '${Math.floor(index/2) + 1}. ${move}' : '${Math.floor(index/2) + 1}... ${move}'`
   - Auto-scroll per D-16: use useRef for container and last move element, useEffect on moves.length change calling `lastMoveRef.current?.scrollIntoView({ behavior: 'smooth', block: 'end' })`
   - Container: `overflow-y-auto max-h-[60vh] bg-secondary p-4 rounded-lg`
   - Heading "Moves" above list (per UI-SPEC copywriting)
   - Each move row: `text-sm leading-relaxed` (14px per UI-SPEC typography)
   - Empty state: show nothing or subtle "(No moves yet)" text
   - Manual scroll allowed naturally (D-17) — auto-scroll only triggers on new moves

3. Create client/src/components/chess/ChessSidebar.tsx:
   - Props: `{ whitePlayerName: string | null; blackPlayerName: string | null; currentTurn: PieceColor; moves: string[]; onSelectWhitePlayer: () => void; onSelectBlackPlayer: () => void; onNewGame: () => void; onUndo: () => void }`
   - Layout: vertical flex column with gap-4, width set by parent container
   - Render in order (per D-09, D-10, D-14):
     1. PlayerBadge for white (isCurrentTurn = currentTurn === 'w')
     2. PlayerBadge for black (isCurrentTurn = currentTurn === 'b')
     3. Turn indicator text: `${currentTurnPlayerName}'s Turn` (per UI-SPEC copywriting CHESS-08)
     4. Button "New Game" (shadcn Button, min-h-12, variant="outline") — calls onNewGame
     5. Button "Undo Move" (shadcn Button, min-h-12, variant="outline", disabled={moves.length === 0}) — calls onUndo (per UI-SPEC: disabled if no moves)
     6. MoveHistory component with moves array
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/chess/PlayerBadge.tsx exports PlayerBadge component
    - PlayerBadge.tsx contains min-h-[48px] for touch target
    - PlayerBadge.tsx displays "White:" or "Black:" label text
    - PlayerBadge.tsx displays "Not Selected" when playerName is null
    - PlayerBadge.tsx contains ring-2 conditional class for current turn highlight
    - client/src/components/chess/MoveHistory.tsx exports MoveHistory component
    - MoveHistory.tsx contains scrollIntoView for auto-scroll
    - MoveHistory.tsx contains useRef for lastMoveRef
    - MoveHistory.tsx contains useEffect with moves.length dependency
    - MoveHistory.tsx contains Math.floor(index/2) for move number calculation
    - MoveHistory.tsx contains overflow-y-auto for scrollable container
    - client/src/components/chess/ChessSidebar.tsx exports ChessSidebar component
    - ChessSidebar.tsx renders two PlayerBadge components
    - ChessSidebar.tsx renders "New Game" button text
    - ChessSidebar.tsx renders "Undo Move" button text
    - ChessSidebar.tsx renders MoveHistory component
    - ChessSidebar.tsx contains disabled condition for Undo button (moves.length === 0)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>PlayerBadge shows tappable player indicators with turn highlight. MoveHistory displays vertical SAN list with auto-scroll. ChessSidebar combines all controls and history in vertical layout.</done>
</task>

</tasks>

<verification>
- All 5 component files exist in client/src/components/chess/
- TypeScript compilation passes with zero errors
- Board renders 64 squares in 8x8 grid
- Sidebar contains player badges, buttons, and move history
- Auto-scroll ref is wired in MoveHistory
</verification>

<success_criteria>
- All components compile without TypeScript errors
- ChessBoard renders 8x8 grid layout
- ChessSquare displays Unicode pieces and handles click events
- MoveHistory formats moves as "1. e4", "1... e5" vertically
- PlayerBadge shows player name with turn indicator
- ChessSidebar arranges all controls in vertical layout
</success_criteria>

<output>
After completion, create `.planning/phases/05-chess-board/05-02-SUMMARY.md`
</output>
