---
phase: 03-chore-system
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - client/src/components/FamilyMemberBadge.tsx
  - client/src/components/ChoreCard.tsx
  - client/src/components/ChoreFormModal.tsx
  - client/src/components/RecurrenceConfig.tsx
  - client/src/components/CompletedSection.tsx
  - client/src/components/CelebrationMessage.tsx
  - client/src/components/ChoreList.tsx
  - client/src/App.tsx
autonomous: true
requirements: [CHOR-01, CHOR-02, CHOR-03, CHOR-04, CHOR-05, CHOR-06, CHOR-07, CHOR-08, CHOR-10, CHOR-12]

must_haves:
  truths:
    - "User can see a unified list of active chores sorted by points descending"
    - "User can toggle between Daily and Weekly views"
    - "User can tap 48px checkbox to complete a chore with default-to-assignee"
    - "User can long-press checkbox to select who completed it"
    - "User can tap Add Chore (PIN-protected) to open creation form"
    - "User can set recurring frequency when creating/editing chores"
    - "Completed chores appear in collapsible Completed section"
    - "All-done celebration message shows when no active chores remain"
    - "Chore cards are color-coded by assignee using chart colors"
  artifacts:
    - path: "client/src/components/ChoreList.tsx"
      provides: "Main chore list view with header, toggle, active list, completed section"
      min_lines: 80
    - path: "client/src/components/ChoreCard.tsx"
      provides: "Individual chore card with 48px checkbox, assignee badge, points"
      min_lines: 40
    - path: "client/src/components/ChoreFormModal.tsx"
      provides: "Add/Edit chore form modal with validation and recurring toggle"
      min_lines: 60
    - path: "client/src/components/CompletedSection.tsx"
      provides: "Collapsible completed chores section"
      min_lines: 30
    - path: "client/src/components/CelebrationMessage.tsx"
      provides: "All done for today celebration display"
      min_lines: 10
    - path: "client/src/App.tsx"
      provides: "App root rendering ChoreList instead of FamilyList"
  key_links:
    - from: "client/src/components/ChoreList.tsx"
      to: "client/src/hooks/useChoreData.ts"
      via: "useChoreData hook call"
      pattern: "useChoreData"
    - from: "client/src/components/ChoreCard.tsx"
      to: "client/src/hooks/useLongPress.ts"
      via: "useLongPress hook for checkbox interaction"
      pattern: "useLongPress"
    - from: "client/src/components/ChoreList.tsx"
      to: "client/src/hooks/usePinAuth.ts"
      via: "withPinAuth for add/edit/delete gating"
      pattern: "withPinAuth"
    - from: "client/src/App.tsx"
      to: "client/src/components/ChoreList.tsx"
      via: "ChoreList render in App"
      pattern: "ChoreList"
---

<objective>
Build all chore UI components and wire them into the app: ChoreList (main view), ChoreCard (individual chore with completion), ChoreFormModal (add/edit with recurring), RecurrenceConfig, CompletedSection, CelebrationMessage, FamilyMemberBadge. Replace FamilyList with ChoreList as the main app view.

Purpose: Delivers the complete chore user interface that family members interact with daily. This is the core user-facing feature of Phase 3.
Output: Fully functional chore management UI with create, complete, undo, delete, daily/weekly views, and recurring configuration.
</objective>

<execution_context>
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chore-system/03-CONTEXT.md
@.planning/phases/03-chore-system/03-UI-SPEC.md
@.planning/phases/03-chore-system/03-01-SUMMARY.md
@.planning/phases/03-chore-system/03-02-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 02 that executor will consume -->

From client/src/types/chore.ts:
```typescript
export interface Chore {
  id: number;
  title: string;
  description: string | null;
  assigned_to: number;
  completed_by: number | null;
  points: number;
  status: 'active' | 'completed' | 'auto_completed';
  is_recurring: number;
  recurrence_config: string | null;
  parent_chore_id: number | null;
  created_at: number;
  completed_at: number | null;
  updated_at: number;
  assignee_name: string;
  completed_by_name: string | null;
}

export interface RecurrenceConfig {
  frequency: 'daily' | 'weekly' | 'custom' | 'interval';
  days: number[];
  interval?: number;
}

export interface ChoreFormData {
  title: string;
  assigned_to: number;
  description?: string;
  points?: number;
  is_recurring?: boolean;
  recurrence_config?: RecurrenceConfig;
}
```

From client/src/hooks/useChoreData.ts:
```typescript
export function useChoreData(view: 'daily' | 'weekly' = 'daily') {
  return {
    chores, activeChores, completedChores, stats,
    loading, error,
    createChore, updateChore, completeChore, undoComplete, deleteChore, refetch
  };
}
```

From client/src/hooks/useLongPress.ts:
```typescript
export function useLongPress({ onLongPress, onClick, threshold = 1000 }) {
  return { onTouchStart, onTouchEnd, onTouchMove, onTouchCancel, onMouseDown, onMouseUp, onMouseMove, onMouseLeave };
}
```

From client/src/hooks/usePinAuth.ts:
```typescript
export function usePinAuth() {
  return { verifyPin, isVerifying, pinError, clearError, withPinAuth, showPinModal, closePinModal };
}
```

From client/src/hooks/useFamilyData.ts:
```typescript
export function useFamilyData() {
  return { members, loading, error, addMember, updateMember, deleteMember, refetch };
}
```

From client/src/types/family.ts:
```typescript
export interface FamilyMember { id: number; name: string; created_at: number; updated_at: number; }
```

Existing components available:
- client/src/components/PinModal.tsx (PIN authentication modal)
- client/src/components/ui/button.tsx (shadcn Button)
- client/src/components/ui/card.tsx (shadcn Card)
- client/src/components/ui/dialog.tsx (shadcn Dialog)
- client/src/components/ui/input.tsx (shadcn Input)
- client/src/components/ui/label.tsx (shadcn Label)
- client/src/components/ui/checkbox.tsx (shadcn Checkbox - installed Plan 02)
- client/src/components/ui/textarea.tsx (shadcn Textarea - installed Plan 02)
- client/src/components/ui/select.tsx (shadcn Select - installed Plan 02)
- client/src/components/ui/toggle-group.tsx (shadcn ToggleGroup - installed Plan 02)
- client/src/components/ui/collapsible.tsx (shadcn Collapsible - installed Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FamilyMemberBadge, ChoreCard, and CompletedSection components</name>
  <files>client/src/components/FamilyMemberBadge.tsx, client/src/components/ChoreCard.tsx, client/src/components/CompletedSection.tsx, client/src/components/CelebrationMessage.tsx</files>
  <read_first>
    - client/src/components/FamilyCard.tsx (existing card component pattern from Phase 2)
    - client/src/components/ui/card.tsx (shadcn Card API)
    - client/src/components/ui/checkbox.tsx (shadcn Checkbox API)
    - client/src/components/ui/collapsible.tsx (shadcn Collapsible API)
    - client/src/hooks/useLongPress.ts (hook API from Plan 02)
    - client/src/types/chore.ts (Chore interface from Plan 02)
    - .planning/phases/03-chore-system/03-UI-SPEC.md (Layout Patterns section for chore card structure, spacing, completed section)
    - .planning/phases/03-chore-system/03-CONTEXT.md (D-07 through D-19 layout and completion decisions)
  </read_first>
  <action>
**Create client/src/components/FamilyMemberBadge.tsx** — Small colored circle with member initial (per D-08, D-09):

- Props: `{ name: string; colorIndex: number; size?: number }` where colorIndex maps to chart-1 through chart-4
- Render a 32px circle with the member's first initial letter, uppercase
- Background color uses CSS variables: `var(--chart-1)` through `var(--chart-4)` based on `colorIndex % 4 + 1`
- Text color: white, font-weight 600, text-sm (14px)
- Exact Tailwind classes: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-semibold text-white`
- Style attribute for dynamic background: `style={{ backgroundColor: \`var(--chart-${(colorIndex % 4) + 1})\` }}`

**Create client/src/components/ChoreCard.tsx** — Individual chore display with completion checkbox:

Props:
```typescript
interface ChoreCardProps {
  chore: Chore;
  colorIndex: number;
  onComplete: (id: number, completedBy?: number) => void;
  onEdit: (chore: Chore) => void;
  onDelete: (id: number) => void;
  familyMembers: FamilyMember[];
}
```

Structure per UI-SPEC Layout Patterns:
- Card component with 4px left border using assignee's chart color: `style={{ borderLeftWidth: '4px', borderLeftColor: \`var(--chart-${(colorIndex % 4) + 1})\` }}`
- Internal padding: `p-4` (16px)
- Flex layout: `flex items-center gap-4`
- Left side: 48px checkbox wrapper using useLongPress hook
  - `div` with `min-w-12 min-h-12 flex items-center justify-center` (48px per CHOR-06)
  - Normal tap (onClick): calls `onComplete(chore.id)` (default-to-assignee per D-16)
  - Long-press (1s per D-17): opens family picker dialog (a simple shadcn Dialog with family member list buttons)
  - Checkbox checked state: `chore.status !== 'active'`
- Center: chore details
  - Title: `text-lg` (18px per UI-SPEC typography)
  - Assignee: `text-sm text-muted-foreground` showing `chore.assignee_name` with FamilyMemberBadge inline
- Right side: points badge + edit/delete actions
  - Points: `text-sm bg-muted px-2 py-1 rounded` showing `${chore.points}pts`
  - Edit button: `min-h-12 min-w-12` touch target with pencil icon, `aria-label="Edit chore"` (per UI-SPEC accessibility)
  - Delete button: `min-h-12 min-w-12` touch target with trash icon, `aria-label="Delete chore"`, destructive variant
- For completed chores (shown in CompletedSection): opacity 0.7, checkbox shows checkmark with assignee's chart color
- For auto_completed (missed) chores: strikethrough text, "Missed" badge in muted color

For the family picker on long-press: render a simple Dialog with title "Who completed this?" listing each family member as a 48px button. When a member is tapped, call `onComplete(chore.id, member.id)` and close the dialog.

Use HugeIcons for edit (pencil-edit-02) and delete (delete-02) icons. Import pattern: `import { PencilEdit02Icon, Delete02Icon } from 'hugeicons-react'`. If hugeicons-react is not installed, use Lucide icons instead: `import { Pencil, Trash2 } from 'lucide-react'`.

**Create client/src/components/CompletedSection.tsx** — Collapsible completed chores (per D-13, D-14):

Props:
```typescript
interface CompletedSectionProps {
  chores: Chore[];
  colorIndexMap: Map<number, number>;
  onEdit: (chore: Chore) => void;
  onDelete: (id: number) => void;
  familyMembers: FamilyMember[];
}
```

- Use shadcn Collapsible component
- Completely hidden when `chores.length === 0` (per D-14): render null
- Collapsed by default (per D-13): `defaultOpen={false}`
- Trigger shows: "Completed (N)" with a chevron that rotates 180deg when expanded
  - Trigger is 48px touch target
  - Style: `mt-8` (32px top margin per UI-SPEC spacing), horizontal rule styling
- Content: list of ChoreCard components for each completed chore
  - Pass a no-op for onComplete (already completed)
  - Cards show with opacity 0.7
- Transition: `transition-all duration-300 ease-in-out` per UI-SPEC animation

**Create client/src/components/CelebrationMessage.tsx** — "All Done for Today!" (per D-27):

Props: none (stateless)

- Only rendered by ChoreList when activeChores.length === 0 and view is 'daily' and total chores exist
- Centered layout: `flex flex-col items-center justify-center py-16`
- Sparkle emoji or icon: scale animation on mount
- Heading: `text-2xl font-semibold` (24px per UI-SPEC) with Noto Serif Variable font: "All Done for Today!"
- Animation: fade-in with scale(0.95 to 1.0) over 300ms per UI-SPEC
- CSS animation class: `animate-in fade-in zoom-in-95 duration-300`
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit src/components/FamilyMemberBadge.tsx src/components/ChoreCard.tsx src/components/CompletedSection.tsx src/components/CelebrationMessage.tsx 2>&1 | head -30 || echo "TypeScript check complete"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/FamilyMemberBadge.tsx contains `var(--chart-` (chart color mapping per D-08)
    - client/src/components/ChoreCard.tsx contains `useLongPress` (long-press hook for D-17)
    - client/src/components/ChoreCard.tsx contains `min-w-12 min-h-12` (48px touch target per CHOR-06)
    - client/src/components/ChoreCard.tsx contains `aria-label="Edit chore"` (accessibility per UI-SPEC)
    - client/src/components/ChoreCard.tsx contains `aria-label="Delete chore"` (accessibility per UI-SPEC)
    - client/src/components/ChoreCard.tsx contains `Who completed this?` (long-press family picker per D-17)
    - client/src/components/CompletedSection.tsx contains `Collapsible` (shadcn component)
    - client/src/components/CompletedSection.tsx contains `Completed` (section header text)
    - client/src/components/CelebrationMessage.tsx contains `All Done for Today!` (per D-27 copywriting)
  </acceptance_criteria>
  <done>ChoreCard renders with 48px checkbox, left color border, points badge, edit/delete icons with ARIA labels, and long-press family picker. CompletedSection collapses/expands with animation, hidden when empty. CelebrationMessage shows fade-in celebration. FamilyMemberBadge renders colored initial circles.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChoreFormModal with RecurrenceConfig</name>
  <files>client/src/components/ChoreFormModal.tsx, client/src/components/RecurrenceConfig.tsx</files>
  <read_first>
    - client/src/components/FamilyFormModal.tsx (existing modal form pattern from Phase 2)
    - client/src/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle)
    - client/src/components/ui/input.tsx (Input component)
    - client/src/components/ui/textarea.tsx (Textarea component from Plan 02)
    - client/src/components/ui/select.tsx (Select component from Plan 02)
    - client/src/components/ui/checkbox.tsx (Checkbox component from Plan 02)
    - client/src/types/chore.ts (ChoreFormData, RecurrenceConfig interfaces)
    - client/src/types/family.ts (FamilyMember interface for assignee picker)
    - .planning/phases/03-chore-system/03-UI-SPEC.md (Add/Edit Chore Form layout pattern, form spacing, labels)
    - .planning/phases/03-chore-system/03-CONTEXT.md (D-10, D-20, D-21 form decisions)
  </read_first>
  <action>
**Create client/src/components/RecurrenceConfig.tsx** — Recurring frequency picker (per D-20, D-21):

Props:
```typescript
interface RecurrenceConfigProps {
  value: RecurrenceConfig;
  onChange: (config: RecurrenceConfig) => void;
}
```

- Only shown when the "Recurring" toggle is checked (handled by ChoreFormModal)
- Frequency selector: shadcn Select with options per D-20:
  - "Daily" (frequency: 'daily', days: [0,1,2,3,4,5,6])
  - "Weekly" (frequency: 'weekly', days: [0,1,2,3,4,5,6])
  - "Custom Days" (frequency: 'custom', days: user-selected)
  - "Every N Days" (frequency: 'interval', interval: user-specified)
- When "Custom Days" selected: show 7 day-of-week checkboxes (Mon/Tue/Wed/Thu/Fri/Sat/Sun)
  - Each checkbox: 48px touch target
  - Days stored as numbers: 0=Sunday, 1=Monday, ..., 6=Saturday
  - Display order: Mon(1), Tue(2), Wed(3), Thu(4), Fri(5), Sat(6), Sun(0)
- When "Every N Days" selected: show number input (1-365 range) with label "Repeat every _ days"
  - Input: `type="number" min={1} max={365}` with 48px height
- Spacing: Label to select: 8px (space-2), Select to options: 16px (space-4)

**Create client/src/components/ChoreFormModal.tsx** — Add/Edit chore form (per D-10, D-21):

Props:
```typescript
interface ChoreFormModalProps {
  open: boolean;
  onClose: () => void;
  onSubmit: (data: ChoreFormData) => Promise<void>;
  familyMembers: FamilyMember[];
  initialData?: Chore; // If provided, editing existing chore
}
```

Layout per UI-SPEC "Add/Edit Chore Form" wireframe:

1. Dialog with header "Add Chore" or "Edit Chore" based on initialData presence
2. Form fields (each with Label + Input, spacing per UI-SPEC: label-to-input 8px, field-to-field 16px):
   - **Chore Title** (required): `<Input placeholder="Enter chore title..." className="min-h-12" />` (per UI-SPEC copywriting)
   - **Assigned To** (required): shadcn Select dropdown with family members, `className="min-h-12"`, placeholder "Select family member..."
   - **Points (Optional)**: `<Input type="number" placeholder="0" min={0} className="min-h-12" />` (per D-01 points integer)
   - **Description (Optional)**: `<Textarea placeholder="Add details..." className="min-h-24" />` (per UI-SPEC 96px min height)
   - **Recurring**: shadcn Checkbox toggle (per D-21, not separate section)
   - If Recurring checked: render `<RecurrenceConfig />` component below

3. Form actions: `gap-2 mt-6`
   - Cancel button: secondary variant, `min-h-12`
   - Submit button: primary accent, `min-h-12`, text "Add Chore" or "Save Changes"

4. Validation:
   - Title: required, show error border + message "Chore title is required" if empty on submit
   - Assigned To: required, show error if not selected
   - Form submitting state: disable inputs, show spinner on submit button
   - On success: close modal (handled by ChoreList calling onClose)
   - On error: show toast "Unable to create chore. Please try again." or "Unable to update chore. Please try again."

5. When editing (initialData provided):
   - Pre-fill all fields from initialData
   - Parse recurrence_config JSON string to populate RecurrenceConfig
   - Submit calls updateChore instead of createChore

Form state management: use useState for each field. Reset form when modal opens/closes.

Per UI-SPEC: form modal is vertically scrollable if content exceeds viewport height. Add `className="max-h-[80vh] overflow-y-auto"` to DialogContent.
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit src/components/ChoreFormModal.tsx src/components/RecurrenceConfig.tsx 2>&1 | head -30 || echo "TypeScript check complete"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/ChoreFormModal.tsx contains `Add Chore` and `Edit Chore` (modal title per UI-SPEC copywriting)
    - client/src/components/ChoreFormModal.tsx contains `Save Changes` (edit mode submit button)
    - client/src/components/ChoreFormModal.tsx contains `Chore Title` (form label per UI-SPEC copywriting)
    - client/src/components/ChoreFormModal.tsx contains `Assigned To` (form label per UI-SPEC copywriting)
    - client/src/components/ChoreFormModal.tsx contains `Description (Optional)` (form label per UI-SPEC copywriting)
    - client/src/components/ChoreFormModal.tsx contains `min-h-12` (48px touch targets on form inputs)
    - client/src/components/ChoreFormModal.tsx contains `RecurrenceConfig` (renders recurring picker when toggle on, per D-21)
    - client/src/components/RecurrenceConfig.tsx contains `Daily` and `Weekly` and `Custom Days` and `Every N Days` (per D-20)
    - client/src/components/RecurrenceConfig.tsx contains `frequency` (RecurrenceConfig type usage)
  </acceptance_criteria>
  <done>ChoreFormModal renders add/edit form with title, assignee select, points, description, and recurring toggle. RecurrenceConfig supports all 4 frequency types with custom day checkboxes and interval input. All form inputs have 48px touch targets. Validation prevents empty title/assignee submission.</done>
</task>

<task type="auto">
  <name>Task 3: Create ChoreList main view and wire into App.tsx</name>
  <files>client/src/components/ChoreList.tsx, client/src/App.tsx</files>
  <read_first>
    - client/src/components/FamilyList.tsx (existing list component pattern from Phase 2)
    - client/src/App.tsx (current app root rendering FamilyList)
    - client/src/components/PinModal.tsx (PIN modal for protected actions)
    - client/src/components/ChoreCard.tsx (from Task 1)
    - client/src/components/ChoreFormModal.tsx (from Task 2)
    - client/src/components/CompletedSection.tsx (from Task 1)
    - client/src/components/CelebrationMessage.tsx (from Task 1)
    - client/src/hooks/useChoreData.ts (data hook from Plan 02)
    - client/src/hooks/usePinAuth.ts (PIN auth hook)
    - client/src/hooks/useFamilyData.ts (family data for assignee list)
    - client/src/utils/date-filters.ts (formatWeekRange for weekly view header)
    - .planning/phases/03-chore-system/03-UI-SPEC.md (Chore List View layout pattern, empty states, interaction patterns)
    - .planning/phases/03-chore-system/03-CONTEXT.md (D-07, D-10, D-11, D-12, D-13, D-14, D-26 through D-29)
  </read_first>
  <action>
**Create client/src/components/ChoreList.tsx** — Main chore list view:

This is the primary component that orchestrates all chore interactions. Structure per UI-SPEC "Chore List View (Daily/Weekly)" wireframe:

```typescript
import { useState } from 'react';
import { useChoreData } from '../hooks/useChoreData';
import { useFamilyData } from '../hooks/useFamilyData';
import { usePinAuth } from '../hooks/usePinAuth';
import { formatWeekRange } from '../utils/date-filters';
import type { Chore, ChoreFormData } from '../types/chore';
// ... component imports
```

**State:**
- `viewMode`: 'daily' | 'weekly' (default 'daily')
- `showAddForm`: boolean (controls ChoreFormModal for new chore)
- `editingChore`: Chore | null (controls ChoreFormModal for editing)
- `deleteTarget`: number | null (chore id pending delete confirmation)

**Hook calls:**
- `const { activeChores, completedChores, stats, loading, error, createChore, updateChore, completeChore, deleteChore } = useChoreData(viewMode);`
- `const { members } = useFamilyData();` (for assignee list in forms + family picker)
- `const { withPinAuth, showPinModal, closePinModal, verifyPin, isVerifying, pinError, clearError } = usePinAuth();`

**Color index map:** Create a stable mapping of family member id to index for consistent coloring:
```typescript
const colorIndexMap = new Map(members.map((m, i) => [m.id, i]));
```

**Layout (top to bottom, page margins `px-6`):**

1. **Header row** (`flex items-center justify-between mb-4`):
   - Left: heading `text-2xl font-semibold` with Noto Serif font
     - Daily view: "Today's Chores"
     - Weekly view: "This Week's Chores"
   - Right: "Add Chore" button, primary variant, `min-h-12`
     - onClick: `withPinAuth(async () => setShowAddForm(true))` (PIN-protected per D-10)

2. **View toggle** (`mb-6`):
   - shadcn ToggleGroup with two items: "Today" and "This Week" (per UI-SPEC copywriting)
   - Each toggle: `min-h-12` (48px touch target)
   - Active: primary accent background, white text
   - `value={viewMode}` and `onValueChange` to switch between 'daily' and 'weekly'
   - Below toggle in weekly view: show week range text `text-sm text-muted-foreground` using `formatWeekRange()`

3. **Content area** (conditional rendering):

   **If loading:** Show skeleton loading (3 card-shaped placeholders with pulse animation)

   **If error:** Show error message per UI-SPEC copywriting: "Unable to load chores. Check your connection and try again." with retry button

   **If no chores exist at all (activeChores.length === 0 and completedChores.length === 0):**
   - Per D-26: just show the "Add Chore" button (already in header), minimal empty state
   - Show heading "No Chores Yet" and body "Tap Add Chore to create your first task." per UI-SPEC copywriting

   **If all active done (activeChores.length === 0 and completedChores.length > 0 and viewMode === 'daily'):**
   - Render `<CelebrationMessage />` per D-27

   **If no chores this week (activeChores.length === 0 and viewMode === 'weekly'):**
   - Per D-28: "No Chores This Week" heading with week date range in body text per UI-SPEC copywriting

   **Normal state (activeChores.length > 0):**
   - Render active chores as ChoreCard list with `gap-2` (8px per UI-SPEC spacing)
   - Each ChoreCard gets:
     - `chore={chore}`
     - `colorIndex={colorIndexMap.get(chore.assigned_to) || 0}`
     - `onComplete={completeChore}` (handles both default and long-press override)
     - `onEdit={(chore) => withPinAuth(async () => setEditingChore(chore))}` (PIN-protected)
     - `onDelete={(id) => withPinAuth(async () => setDeleteTarget(id))}` (PIN-protected per CHOR-04)
     - `familyMembers={members}`

4. **Completed section** (always below active list):
   - `<CompletedSection chores={completedChores} colorIndexMap={colorIndexMap} familyMembers={members} />`
   - Hidden when no completed chores (handled internally per D-14)

5. **Modals:**
   - `<PinModal open={showPinModal} onClose={closePinModal} onSubmit={verifyPin} isVerifying={isVerifying} error={pinError} onClearError={clearError} />`
   - `<ChoreFormModal open={showAddForm} onClose={() => setShowAddForm(false)} onSubmit={createChore} familyMembers={members} />`
   - `<ChoreFormModal open={!!editingChore} onClose={() => setEditingChore(null)} onSubmit={(data) => updateChore(editingChore!.id, data)} familyMembers={members} initialData={editingChore || undefined} />`
   - Delete confirmation dialog (simple Dialog with "Delete This Chore?" heading, "This chore will be permanently removed. This action cannot be undone." body, "Delete" destructive button, "Keep Chore" cancel button per UI-SPEC copywriting). On confirm: call `deleteChore(deleteTarget!)` and set `deleteTarget = null`.

6. **Stats display** (per CHOR-11):
   - Below the header or inline: show completed count per family member
   - Format: `${member_name}: ${completed_count} done` for each member with completed chores
   - Use FamilyMemberBadge with count badge
   - Only show if stats array has entries

**Update client/src/App.tsx** — Replace FamilyList with ChoreList as the main view:

```typescript
import { ChoreList } from './components/ChoreList';

export default function App() {
  return (
    <main className="min-h-screen bg-background">
      <ChoreList />
    </main>
  );
}
```

Note: FamilyList import is removed. Family management will be accessible through a future navigation system (Phase 6 polish). For now, chore system is the primary view.
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit src/components/ChoreList.tsx src/App.tsx 2>&1 | head -30 && echo "TypeScript check complete"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/ChoreList.tsx contains `useChoreData` (data hook integration)
    - client/src/components/ChoreList.tsx contains `usePinAuth` (PIN-protected actions per D-10)
    - client/src/components/ChoreList.tsx contains `useFamilyData` (family members for assignee list)
    - client/src/components/ChoreList.tsx contains `Today's Chores` (daily view heading per UI-SPEC)
    - client/src/components/ChoreList.tsx contains `This Week's Chores` (weekly view heading)
    - client/src/components/ChoreList.tsx contains `Add Chore` (primary CTA per UI-SPEC copywriting)
    - client/src/components/ChoreList.tsx contains `No Chores Yet` (empty state per D-26, UI-SPEC)
    - client/src/components/ChoreList.tsx contains `No Chores This Week` (weekly empty state per D-28)
    - client/src/components/ChoreList.tsx contains `Delete This Chore?` (delete confirmation per UI-SPEC copywriting)
    - client/src/components/ChoreList.tsx contains `Keep Chore` (cancel button per UI-SPEC copywriting)
    - client/src/components/ChoreList.tsx contains `CelebrationMessage` (all-done state per D-27)
    - client/src/components/ChoreList.tsx contains `CompletedSection` (completed chores section)
    - client/src/components/ChoreList.tsx contains `ChoreFormModal` (add/edit form)
    - client/src/components/ChoreList.tsx contains `PinModal` (PIN auth)
    - client/src/components/ChoreList.tsx contains `ToggleGroup` (daily/weekly view toggle)
    - client/src/App.tsx contains `ChoreList` (main view replacement)
    - client/src/App.tsx does NOT contain `FamilyList` (replaced)
  </acceptance_criteria>
  <done>ChoreList renders the complete chore management UI with header, view toggle, active chore list, completed section, celebration message, and all modals (PIN, add/edit form, delete confirmation). App.tsx renders ChoreList as the primary view. All empty states match UI-SPEC copywriting. All interactive elements have 48px touch targets.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd client && npx tsc --noEmit`
2. Vite dev build succeeds: `cd client && npx vite build --mode development` exits 0
3. All component files exist and are non-empty
4. App.tsx renders ChoreList (not FamilyList)
5. All UI-SPEC copywriting text present in source (exact strings from copywriting contract)
6. All interactive elements have min-h-12 class (48px touch targets)
</verification>

<success_criteria>
- Complete chore UI visible: header, view toggle, chore cards, completed section
- PIN-protected Add Chore button per D-10
- 48px checkbox on each chore card per CHOR-06
- Long-press opens family picker per D-17
- Color-coded cards by assignee per D-08
- Daily/Weekly view toggle per D-12
- Completed section collapsible and hidden when empty per D-13, D-14
- "All Done for Today!" celebration per D-27
- All empty states match UI-SPEC copywriting contract
- Delete confirmation dialog with correct copy
</success_criteria>

<output>
After completion, create `.planning/phases/03-chore-system/03-03-SUMMARY.md`
</output>
