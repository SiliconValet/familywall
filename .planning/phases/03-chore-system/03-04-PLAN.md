---
phase: 03-chore-system
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - client/src/components/WeeklySummary.tsx
  - client/src/components/ChoreList.tsx
autonomous: false
requirements: [CHOR-05, CHOR-11]

must_haves:
  truths:
    - "User can open Weekly Summary view showing completion grid for the current week"
    - "User can see completed chore count per family member"
    - "Weekly summary shows checkmark/X/dash for each day per recurring chore"
    - "User can close weekly summary via X button or backdrop tap"
  artifacts:
    - path: "client/src/components/WeeklySummary.tsx"
      provides: "Full-screen overlay with weekly completion grid"
      min_lines: 40
  key_links:
    - from: "client/src/components/ChoreList.tsx"
      to: "client/src/components/WeeklySummary.tsx"
      via: "WeeklySummary render triggered by button"
      pattern: "WeeklySummary"
    - from: "client/src/components/WeeklySummary.tsx"
      to: "/api/chores/summary"
      via: "fetch for weekly summary data"
      pattern: "fetch.*api/chores/summary"
---

<objective>
Build the Weekly Summary report component and integrate it into ChoreList. Then verify the complete chore system visually.

Purpose: Provides parents with a read-only week-at-a-glance view showing which recurring chores were completed, missed, or not scheduled for each day. Also serves as the final integration checkpoint.
Output: WeeklySummary component accessible from weekly view, plus visual verification of the complete chore system.
</objective>

<execution_context>
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chore-system/03-CONTEXT.md
@.planning/phases/03-chore-system/03-UI-SPEC.md
@.planning/phases/03-chore-system/03-01-SUMMARY.md
@.planning/phases/03-chore-system/03-02-SUMMARY.md
@.planning/phases/03-chore-system/03-03-SUMMARY.md

<interfaces>
From client/src/types/chore.ts:
```typescript
export interface WeeklySummaryDay {
  date: string;
  status: 'completed' | 'missed' | 'not_scheduled';
}

export interface WeeklySummaryRow {
  chore_title: string;
  assigned_to: number;
  assignee_name: string;
  days: WeeklySummaryDay[];
}
```

From API endpoint GET /api/chores/summary:
Returns array of WeeklySummaryRow objects.

From client/src/utils/date-filters.ts:
```typescript
export function formatWeekRange(date?: Date): string; // "March 22-28"
export function getWeekDays(date?: Date): Date[]; // Array of 7 dates
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeeklySummary component and integrate into ChoreList</name>
  <files>client/src/components/WeeklySummary.tsx, client/src/components/ChoreList.tsx</files>
  <read_first>
    - client/src/components/ChoreList.tsx (existing main view to add summary button)
    - client/src/types/chore.ts (WeeklySummaryRow, WeeklySummaryDay interfaces)
    - client/src/utils/date-filters.ts (formatWeekRange, getWeekDays helpers)
    - client/src/components/FamilyMemberBadge.tsx (reuse for colored assignee indicators)
    - client/src/components/ui/dialog.tsx (Dialog component for overlay)
    - .planning/phases/03-chore-system/03-UI-SPEC.md (Weekly Summary Report section)
    - .planning/phases/03-chore-system/03-CONTEXT.md (D-24 weekly summary)
  </read_first>
  <action>
**Create client/src/components/WeeklySummary.tsx** (per D-24, UI-SPEC Weekly Summary Report):

Props:
```typescript
interface WeeklySummaryProps {
  open: boolean;
  onClose: () => void;
  colorIndexMap: Map<number, number>;
}
```

- Uses shadcn Dialog as full-screen overlay
- Fetches data from GET /api/chores/summary on open (useEffect when open=true)
- Loading state while fetching

**Layout per UI-SPEC:**
- Dialog header: "Weekly Summary" heading (`text-2xl font-semibold`) with week range subtitle using `formatWeekRange()`
- Close button: 48px touch target (X icon) with `aria-label="Close summary"`

**Grid structure:**
- Column headers: Mon / Tue / Wed / Thu / Fri / Sat / Sun (abbreviated day names)
- One row per recurring chore returned by API
- Each row shows:
  - Left: chore title + FamilyMemberBadge for assignee (color-coded per chart colors)
  - 7 cells for each day of the week:
    - Completed: green checkmark character or Lucide Check icon, color `text-green-600`
    - Missed (auto_completed): red X character or Lucide X icon, color `text-red-500`
    - Not scheduled: em dash in `text-muted-foreground`
- Color-coded rows by assignee using chart color as left border (same pattern as ChoreCard)

**Styling:**
- Grid: `grid grid-cols-[1fr_repeat(7,_48px)]` for responsive column sizing
- Each cell: centered text, 48px width
- Row gap: 8px (space-2)
- Scrollable content if many chores: `max-h-[70vh] overflow-y-auto`

**Read-only:** No interactive elements except close button and backdrop tap to dismiss.

**Update client/src/components/ChoreList.tsx:**
- Add `const [showSummary, setShowSummary] = useState(false);`
- Add "Weekly Summary" button visible only in weekly view (`viewMode === 'weekly'`)
  - Button: secondary variant, `min-h-12`, text "Weekly Summary" per UI-SPEC copywriting
  - onClick: `setShowSummary(true)`
  - Position: below view toggle, right-aligned
- Render `<WeeklySummary open={showSummary} onClose={() => setShowSummary(false)} colorIndexMap={colorIndexMap} />`
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit src/components/WeeklySummary.tsx src/components/ChoreList.tsx 2>&1 | head -20 && echo "TypeScript check complete"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/WeeklySummary.tsx contains `Weekly Summary` (heading per UI-SPEC copywriting)
    - client/src/components/WeeklySummary.tsx contains `api/chores/summary` (fetch from summary endpoint)
    - client/src/components/WeeklySummary.tsx contains `aria-label="Close summary"` (accessibility)
    - client/src/components/WeeklySummary.tsx contains `completed` and `missed` and `not_scheduled` (status rendering)
    - client/src/components/ChoreList.tsx contains `WeeklySummary` (component import and render)
    - client/src/components/ChoreList.tsx contains `showSummary` (state for toggling summary view)
  </acceptance_criteria>
  <done>WeeklySummary renders full-screen overlay with weekly completion grid. ChoreList shows "Weekly Summary" button in weekly view mode. Grid shows checkmarks for completed, X for missed, dash for not-scheduled days. Close via X button or backdrop tap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete chore system</name>
  <files>N/A</files>
  <action>
User visually verifies the complete chore management system end-to-end. Start backend (`cd server && node index.js`) and frontend (`cd client && npx vite`). Open browser to http://localhost:5173.

Test all chore flows: creation (PIN-protected), completion (48px checkbox), undo (5s toast), long-press override, daily/weekly toggle, completed section, delete (PIN-protected), recurring setup, empty states, color coding, and weekly summary.
  </action>
  <verify>User confirms "approved" or describes issues to fix.</verify>
  <done>User has verified all chore system functionality works correctly on screen.</done>
  <what-built>Complete chore management system: backend API with CRUD/completion/undo endpoints, recurring chore cron job, frontend with chore list, daily/weekly views, 48px completion checkboxes, long-press override, add/edit/delete modals (PIN-protected), completed section, celebration message, and weekly summary report.</what-built>
  <how-to-verify>
1. Start the backend: `cd server && node index.js`
2. Start the frontend: `cd client && npx vite` (opens on localhost:5173)
3. Open browser to http://localhost:5173

**Test chore creation (CHOR-01, CHOR-02):**
- Tap "Add Chore" button at top right
- Enter PIN (1234 is default)
- Fill in: Title "Take out trash", Assignee (select a family member), Points 5
- Tap "Add Chore" -- chore should appear in list

**Test completion (CHOR-03, CHOR-06, CHOR-12):**
- Tap the checkbox (should be large, 48px+)
- Verify: checkbox animates, card fades, toast appears "Chore completed" with Undo button
- Toast should stay for 5 seconds
- Tap "Undo" -- chore should return to active list

**Test long-press (D-17):**
- Hold checkbox for 1 second
- "Who completed this?" picker should appear with family members
- Select a member -- chore completes with that member credited

**Test daily/weekly toggle (D-12):**
- Toggle between "Today" and "This Week"
- Header should change between "Today's Chores" and "This Week's Chores"

**Test completed section (CHOR-07, D-13, D-14):**
- Complete a chore
- "Completed (1)" section should appear below active list
- Tap to expand -- should show completed chore at reduced opacity

**Test delete (CHOR-04):**
- Tap trash icon on a chore card
- Enter PIN
- Confirmation dialog should show "Delete This Chore?"
- Tap "Delete" -- chore removed

**Test recurring (CHOR-08):**
- Add a new chore with "Recurring" toggle enabled
- Select "Daily" frequency
- Verify chore is created with recurring indicator

**Test empty states:**
- Complete all active chores -- should show "All Done for Today!" celebration
- Switch to weekly view with no chores -- should show "No Chores This Week"

**Test color coding (D-08):**
- Create chores assigned to different family members
- Each chore card should have a different colored left border
  </how-to-verify>
  <resume-signal>Type "approved" if chore system works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. WeeklySummary component compiles: `cd client && npx tsc --noEmit`
2. Full build succeeds: `cd client && npx vite build --mode development`
3. Server starts without errors with cron job message
4. All chore CRUD operations work end-to-end
5. Visual verification confirms all UI-SPEC layout patterns are followed
</verification>

<success_criteria>
- Weekly Summary shows completion grid for current week per D-24
- Completion counts visible per family member per CHOR-11
- All 12 CHOR requirements demonstrable in the running application
- User confirms chore system works correctly via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-chore-system/03-04-SUMMARY.md`
</output>
