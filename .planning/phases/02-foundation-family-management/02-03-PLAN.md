---
phase: 02-foundation-family-management
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - client/src/App.tsx
  - client/src/components/FamilyList.tsx
  - client/src/components/FamilyCard.tsx
  - client/src/components/FamilyFormModal.tsx
  - client/src/components/PinModal.tsx
  - client/src/components/DeleteConfirmModal.tsx
  - client/src/components/ChangePinModal.tsx
  - client/src/main.tsx
autonomous: false
requirements: [FAM-01, FAM-02, FAM-03, FAM-04, FAM-05, FAM-06, FAM-07, FAM-08]

must_haves:
  truths:
    - "User sees list of family members sorted alphabetically"
    - "User can tap Add to open form modal, enter name, and save"
    - "User can tap Edit on a card to open form modal with pre-filled name"
    - "User can tap Delete and confirm via PIN then confirmation dialog"
    - "All add/edit/delete actions require PIN authentication first"
    - "All buttons and inputs are minimum 44px touch targets"
    - "Success toasts appear after add, edit, and delete operations"
    - "Empty state shown when no family members exist"
    - "User can change parental PIN via settings gear icon"
  artifacts:
    - path: "client/src/components/FamilyList.tsx"
      provides: "Main family member list with header and add button"
      min_lines: 40
    - path: "client/src/components/FamilyCard.tsx"
      provides: "Individual family member card with edit/delete buttons"
      min_lines: 20
    - path: "client/src/components/FamilyFormModal.tsx"
      provides: "Add/Edit form in a dialog modal"
      min_lines: 40
    - path: "client/src/components/PinModal.tsx"
      provides: "PIN entry with numeric keypad"
      min_lines: 60
    - path: "client/src/components/DeleteConfirmModal.tsx"
      provides: "Delete confirmation dialog"
      min_lines: 20
    - path: "client/src/components/ChangePinModal.tsx"
      provides: "PIN change form for settings"
      min_lines: 30
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/components/FamilyList.tsx"
      via: "JSX render"
      pattern: "<FamilyList"
    - from: "client/src/components/FamilyList.tsx"
      to: "client/src/hooks/useFamilyData.ts"
      via: "hook call"
      pattern: "useFamilyData()"
    - from: "client/src/components/FamilyList.tsx"
      to: "client/src/hooks/usePinAuth.ts"
      via: "hook call"
      pattern: "usePinAuth()"
    - from: "client/src/components/ChangePinModal.tsx"
      to: "/api/settings/pin"
      via: "fetch PUT"
      pattern: "fetch.*api/settings/pin"
---

<objective>
Build the complete family management UI: family member list, add/edit form modal, PIN authentication modal with numeric keypad, delete confirmation dialog, PIN change settings, and wire everything together in App.tsx.

Purpose: This is the user-facing feature. All interactions from the UI-SPEC come alive here, including the ability to change the parental PIN (FAM-06).
Output: Fully interactive family management page with PIN-protected CRUD operations and PIN change settings.
</objective>

<execution_context>
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jassmith/Projects/gsdTest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foundation-family-management/02-CONTEXT.md
@.planning/phases/02-foundation-family-management/02-UI-SPEC.md
@.planning/phases/02-foundation-family-management/02-01-SUMMARY.md
@.planning/phases/02-foundation-family-management/02-02-SUMMARY.md

<interfaces>
From client/src/types/family.ts (created in Plan 02):
```typescript
export interface FamilyMember {
  id: number;
  name: string;
  created_at: number;
  updated_at: number;
}
```

From client/src/hooks/useFamilyData.ts (created in Plan 02):
```typescript
export function useFamilyData(): {
  members: FamilyMember[];
  loading: boolean;
  error: string | null;
  addMember: (name: string) => Promise<void>;
  updateMember: (id: number, name: string) => Promise<void>;
  deleteMember: (id: number) => Promise<void>;
  refetch: () => Promise<void>;
}
```

From client/src/hooks/usePinAuth.ts (created in Plan 02):
```typescript
export function usePinAuth(): {
  verifyPin: (pin: string) => Promise<boolean>;
  isVerifying: boolean;
  pinError: string | null;
  clearError: () => void;
  withPinAuth: (action: () => Promise<void>) => void;
  showPinModal: boolean;
  closePinModal: () => void;
}
```

From server/routes/auth.js (created in Plan 01):
```
PUT /api/settings/pin — body: { currentPin: string, newPin: string } — returns { success: true } or 403
```

shadcn components available:
- Button from @/components/ui/button
- Input from @/components/ui/input
- Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter from @/components/ui/dialog
- Card, CardContent from @/components/ui/card
- Label from @/components/ui/label
- toast from sonner (Toaster component)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PinModal and form/confirmation dialog components</name>
  <files>client/src/components/PinModal.tsx, client/src/components/FamilyFormModal.tsx, client/src/components/DeleteConfirmModal.tsx</files>
  <read_first>
    - client/src/hooks/usePinAuth.ts (props interface for PinModal)
    - client/src/types/family.ts (FamilyMember type for form)
    - client/src/components/ui/dialog.tsx (Dialog component API)
    - client/src/components/ui/button.tsx (Button component API)
    - client/src/components/ui/input.tsx (Input component API)
    - .planning/phases/02-foundation-family-management/02-UI-SPEC.md (PIN Modal layout, Form layout, copywriting, spacing)
  </read_first>
  <action>
1. Create `client/src/components/PinModal.tsx`:
   - Props: `open: boolean`, `onClose: () => void`, `onVerify: (pin: string) => Promise<boolean>`, `isVerifying: boolean`, `error: string | null`, `onClearError: () => void`
   - Uses Dialog from shadcn with `open` and `onOpenChange` props
   - DialogTitle: "Enter Parental PIN" (per UI-SPEC copywriting)
   - DialogDescription: "This action requires parental authorization." (per UI-SPEC copywriting)
   - PIN display: 4 circles/dots showing entered digits count, using `min-h-11` sizing
   - Numeric keypad: 3x4 grid layout using CSS grid (`grid-cols-3`, `gap-2`)
   - Each number button: `w-14 h-14` (56px per UI-SPEC for high-precision) with `touch-target` class
   - Buttons for digits 1-9, empty cell, 0, backspace (backspace icon or "X" text)
   - On digit press: append to PIN string state, when 4 digits entered auto-call `onVerify(pin)`
   - On backspace: remove last digit
   - Error display: below keypad in destructive color text, shows `error` prop value ("Incorrect PIN. Please try again." per UI-SPEC)
   - After error, clear PIN digits to allow retry
   - Cancel button at bottom: 44px minimum, calls `onClose`

2. Create `client/src/components/FamilyFormModal.tsx`:
   - Props: `open: boolean`, `onClose: () => void`, `onSubmit: (name: string) => Promise<void>`, `initialName?: string`, `mode: 'add' | 'edit'`
   - Uses Dialog with controlled `open` prop (per D-05, D-06: modal form pattern)
   - DialogTitle: "Add Family Member" or "Edit Family Member" based on mode
   - Form with single `Input` for name, Label "Name" above it
   - Input has `min-h-11` (44px), placeholder "Family member name", `autoFocus` when modal opens
   - Client-side validation: empty name shows "Name is required", >100 chars shows "Name must be less than 100 characters" (per UI-SPEC copywriting)
   - Validate on blur, not on every keystroke (per UI-SPEC Form Validation)
   - Error message below input in destructive color
   - DialogFooter: Cancel button (secondary variant, 44px) and Submit button (primary variant, 44px) (per D-07)
   - Submit button text: "Add Member" in add mode, "Save" in edit mode
   - Loading state: submit button shows "Saving..." text, inputs disabled
   - On successful submit: call `onSubmit(name.trim())`, close modal
   - On error: show "Failed to save. Please try again." below form

3. Create `client/src/components/DeleteConfirmModal.tsx`:
   - Props: `open: boolean`, `onClose: () => void`, `onConfirm: () => Promise<void>`, `memberName: string`
   - Uses Dialog with controlled `open` prop
   - DialogTitle: `Delete ${memberName}?` (per UI-SPEC copywriting)
   - DialogDescription: "This family member will be permanently removed. This action cannot be undone." (per UI-SPEC copywriting)
   - DialogFooter: Cancel button (secondary, 44px text "Cancel") and Delete button (destructive variant, 44px text "Delete") (per UI-SPEC copywriting)
   - Loading state on Delete button while action executes
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit src/components/PinModal.tsx src/components/FamilyFormModal.tsx src/components/DeleteConfirmModal.tsx 2>&1; EXIT=$?; echo "TypeScript exit: $EXIT"; if [ $EXIT -eq 0 ]; then node -e "
const fs = require('fs');

// PinModal behavioral checks
const pin = fs.readFileSync('src/components/PinModal.tsx', 'utf8');
const pinChecks = {
  'has Dialog': pin.includes('Dialog'),
  'has title text': pin.includes('Enter Parental PIN'),
  'has auth description': pin.includes('parental authorization'),
  'has numeric grid': pin.includes('grid-cols-3'),
  'has 56px buttons': pin.includes('w-14') && pin.includes('h-14'),
  'has touch-target': pin.includes('touch-target'),
  'auto-submits on 4 digits': pin.includes('onVerify') || pin.includes('verif'),
};

// FamilyFormModal behavioral checks
const form = fs.readFileSync('src/components/FamilyFormModal.tsx', 'utf8');
const formChecks = {
  'has add/edit modes': form.includes('add') && form.includes('edit'),
  'has name validation': form.includes('Name is required'),
  'has 44px input': form.includes('min-h-11'),
  'has submit handler': form.includes('onSubmit'),
  'has Cancel+Save buttons': form.includes('Cancel') && (form.includes('Save') || form.includes('Add Member')),
};

// DeleteConfirmModal behavioral checks
const del = fs.readFileSync('src/components/DeleteConfirmModal.tsx', 'utf8');
const delChecks = {
  'has destructive button': del.includes('destructive'),
  'has permanence warning': del.includes('permanently removed'),
  'has confirm handler': del.includes('onConfirm'),
  'has cancel option': del.includes('Cancel'),
};

let allOk = true;
for (const [label, ok] of [...Object.entries(pinChecks), ...Object.entries(formChecks), ...Object.entries(delChecks)]) {
  console.log((ok ? 'PASS' : 'FAIL') + ': ' + label);
  if (!ok) allOk = false;
}
process.exit(allOk ? 0 : 1);
"; else exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - PinModal.tsx contains `Enter Parental PIN` (exact copy from UI-SPEC)
    - PinModal.tsx contains `This action requires parental authorization` (exact copy)
    - PinModal.tsx contains `grid-cols-3` (3-column PIN pad layout)
    - PinModal.tsx contains `w-14 h-14` or equivalent 56px sizing for PIN buttons
    - PinModal.tsx contains `touch-target` class usage
    - FamilyFormModal.tsx contains `Name is required` (exact validation message)
    - FamilyFormModal.tsx contains `mode` prop handling for add/edit
    - FamilyFormModal.tsx contains `min-h-11` for input sizing
    - DeleteConfirmModal.tsx contains `permanently removed` (exact copy from UI-SPEC)
    - DeleteConfirmModal.tsx contains `destructive` variant on delete button
    - All three files import from `@/components/ui/dialog`
  </acceptance_criteria>
  <done>Three modal components created: PinModal with numeric keypad (56px buttons, auto-submit on 4 digits), FamilyFormModal with validation (add/edit modes), DeleteConfirmModal with destructive styling. All use shadcn Dialog and meet 44px touch targets.</done>
</task>

<task type="auto">
  <name>Task 2: Create FamilyCard, FamilyList, ChangePinModal, and wire App.tsx</name>
  <files>client/src/components/FamilyCard.tsx, client/src/components/FamilyList.tsx, client/src/components/ChangePinModal.tsx, client/src/App.tsx, client/src/main.tsx</files>
  <read_first>
    - client/src/components/PinModal.tsx (just created - understand props)
    - client/src/components/FamilyFormModal.tsx (just created - understand props)
    - client/src/components/DeleteConfirmModal.tsx (just created - understand props)
    - client/src/hooks/useFamilyData.ts (hook API)
    - client/src/hooks/usePinAuth.ts (hook API)
    - client/src/App.tsx (current placeholder to replace)
    - client/src/main.tsx (need to add Toaster)
    - .planning/phases/02-foundation-family-management/02-UI-SPEC.md (Family List View layout, spacing, empty state)
  </read_first>
  <action>
1. Create `client/src/components/FamilyCard.tsx`:
   - Props: `member: FamilyMember`, `onEdit: (member: FamilyMember) => void`, `onDelete: (member: FamilyMember) => void`
   - Uses shadcn Card component
   - Card has `min-h-14` (56px per UI-SPEC), padding `p-4` (16px per UI-SPEC)
   - Displays member name in `text-lg font-normal` (18px per typography spec)
   - Right side: Edit button (secondary variant, `touch-target` class, text "Edit") and Delete icon button (ghost variant, destructive text color, `touch-target` class)
   - Use flex layout: `flex items-center justify-between`
   - Card gap from next card: handled by parent with `gap-2` (8px per UI-SPEC)

2. Create `client/src/components/ChangePinModal.tsx` (per D-02, FAM-06):
   - Props: `open: boolean`, `onClose: () => void`
   - Uses Dialog with controlled `open` prop
   - DialogTitle: "Change Parental PIN"
   - DialogDescription: "Enter your current PIN and choose a new one."
   - Form with three fields, each using shadcn Input with `min-h-11` (44px), `type="password"` and `inputMode="numeric"` and `maxLength={6}`:
     - "Current PIN" (Label + Input, name `currentPin`)
     - "New PIN" (Label + Input, name `newPin`)
     - "Confirm New PIN" (Label + Input, name `confirmPin`)
   - Client-side validation:
     - All fields required: "This field is required"
     - newPin must be 4-6 digits: "PIN must be 4-6 digits"
     - confirmPin must match newPin: "PINs do not match"
   - On submit: call `PUT /api/settings/pin` with `{ currentPin, newPin }` via fetch
     - On 403 response: show "Current PIN is incorrect" error (per D-02: requires current PIN to change)
     - On success: show toast "PIN changed successfully", close modal
     - On network error: show "Failed to change PIN. Please try again."
   - DialogFooter: Cancel button (secondary, 44px) and "Change PIN" button (primary, 44px)
   - Loading state on submit button while request in flight

3. Create `client/src/components/FamilyList.tsx`:
   - Uses `useFamilyData()` and `usePinAuth()` hooks
   - State: `showAddModal`, `editingMember`, `deletingMember`, `showChangePinModal` (for modal control)
   - Header section: `font-heading text-2xl font-semibold` title "Family Members" with two buttons right-aligned:
     - Settings gear icon button (ghost variant, `touch-target` class) that opens ChangePinModal directly (no PIN required to open settings, PIN is verified inside the form via currentPin field)
     - "Add Family Member" primary Button (per UI-SPEC CTA copy), `touch-target` class
   - Loading state: show "Loading..." centered text
   - Error state: show error message with "Try Again" button (per UI-SPEC error copy: "Unable to load family members. Check your connection and try again.")
   - Empty state: centered content with heading "No Family Members Yet" (`text-2xl font-semibold`) and body "Add your first family member to get started. Tap the button below to begin." (`text-muted-foreground`) and "Add Family Member" button (per UI-SPEC copywriting)
   - List: renders FamilyCard for each member in `flex flex-col gap-2` container
   - Spacing: header to first card `mt-6` (24px per UI-SPEC), page padding `px-6` (24px)
   - Add flow: tap "Add Family Member" -> `withPinAuth(() => setShowAddModal(true))` -> PinModal appears -> on PIN success -> FamilyFormModal appears in add mode -> on submit calls `addMember(name)` -> toast "Family member added successfully" (3s) -> modal closes
   - Edit flow: tap Edit on card -> `withPinAuth(() => setEditingMember(member))` -> PinModal -> FamilyFormModal in edit mode with `initialName={member.name}` -> on submit calls `updateMember(id, name)` -> toast "Family member updated successfully" -> modal closes
   - Delete flow: tap Delete on card -> `withPinAuth(() => setDeletingMember(member))` -> PinModal -> DeleteConfirmModal with memberName -> on confirm calls `deleteMember(id)` -> toast "Family member deleted" -> modal closes
   - Settings flow: tap gear icon -> `setShowChangePinModal(true)` -> ChangePinModal opens -> user enters current + new PIN -> on success toast "PIN changed successfully"
   - Toast messages use `toast()` from sonner library (import `{ toast } from 'sonner'`)
   - Render ChangePinModal: `<ChangePinModal open={showChangePinModal} onClose={() => setShowChangePinModal(false)} />`

4. Replace `client/src/App.tsx` entirely:
```tsx
import { FamilyList } from './components/FamilyList';

export default function App() {
  return (
    <main className="min-h-screen bg-background">
      <FamilyList />
    </main>
  );
}
```

5. Update `client/src/main.tsx` to include the Toaster component:
   - Add `import { Toaster } from 'sonner'` (or from `@/components/ui/sonner` if shadcn wrapped it)
   - Render `<Toaster position="top-right" duration={3000} />` alongside `<App />`
   - Keep existing StrictMode wrapper
  </action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npx tsc --noEmit 2>&1; EXIT=$?; echo "TypeScript exit: $EXIT"; if [ $EXIT -eq 0 ]; then node -e "
const fs = require('fs');
let allOk = true;
function check(label, ok) { console.log((ok ? 'PASS' : 'FAIL') + ': ' + label); if (!ok) allOk = false; }

// FamilyCard: renders member name and has edit/delete buttons
const card = fs.readFileSync('src/components/FamilyCard.tsx', 'utf8');
check('FamilyCard has touch-target', card.includes('touch-target'));
check('FamilyCard has Edit button', card.includes('Edit'));
check('FamilyCard uses Card component', card.includes('Card'));

// FamilyList: wires hooks, renders list, has empty state
const list = fs.readFileSync('src/components/FamilyList.tsx', 'utf8');
check('FamilyList calls useFamilyData', list.includes('useFamilyData()'));
check('FamilyList calls usePinAuth', list.includes('usePinAuth()'));
check('FamilyList has empty state', list.includes('No Family Members Yet'));
check('FamilyList has Add button', list.includes('Add Family Member'));
check('FamilyList uses withPinAuth', list.includes('withPinAuth'));
check('FamilyList shows add toast', list.includes('Family member added successfully'));
check('FamilyList shows edit toast', list.includes('Family member updated successfully'));
check('FamilyList shows delete toast', list.includes('Family member deleted'));
check('FamilyList renders ChangePinModal', list.includes('ChangePinModal'));
check('FamilyList has showChangePinModal state', list.includes('showChangePinModal'));

// ChangePinModal: calls PUT /api/settings/pin
const changePin = fs.readFileSync('src/components/ChangePinModal.tsx', 'utf8');
check('ChangePinModal has API call', changePin.includes('api/settings/pin'));
check('ChangePinModal has title', changePin.includes('Change Parental PIN'));
check('ChangePinModal has currentPin field', changePin.includes('currentPin'));
check('ChangePinModal shows wrong PIN error', changePin.includes('Current PIN is incorrect'));
check('ChangePinModal shows success toast', changePin.includes('PIN changed successfully'));

// App.tsx: renders FamilyList
const app = fs.readFileSync('src/App.tsx', 'utf8');
check('App renders FamilyList', app.includes('<FamilyList'));
check('App removed old placeholder', !app.includes('FamilyWall Kiosk'));

// main.tsx: has Toaster
const main = fs.readFileSync('src/main.tsx', 'utf8');
check('main.tsx has Toaster', main.includes('Toaster'));

process.exit(allOk ? 0 : 1);
"; else exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - FamilyCard.tsx contains `min-h-14` or equivalent 56px height
    - FamilyCard.tsx contains `touch-target` class
    - FamilyCard.tsx contains `text-lg` for member name
    - FamilyList.tsx contains `useFamilyData()` call
    - FamilyList.tsx contains `usePinAuth()` call
    - FamilyList.tsx contains `No Family Members Yet` (exact empty state heading)
    - FamilyList.tsx contains `Add Family Member` button text
    - FamilyList.tsx contains `withPinAuth` calls wrapping add/edit/delete actions
    - FamilyList.tsx contains `toast(` calls for success messages
    - FamilyList.tsx contains `Family member added successfully` toast text
    - FamilyList.tsx contains `Family member updated successfully` toast text
    - FamilyList.tsx contains `Family member deleted` toast text
    - FamilyList.tsx contains `showChangePinModal` state
    - FamilyList.tsx contains `<ChangePinModal` render
    - ChangePinModal.tsx contains `api/settings/pin` fetch call
    - ChangePinModal.tsx contains `Change Parental PIN` title
    - ChangePinModal.tsx contains `currentPin` field
    - ChangePinModal.tsx contains `Current PIN is incorrect` error message
    - ChangePinModal.tsx contains `PIN changed successfully` toast text
    - App.tsx contains `<FamilyList` component
    - App.tsx does NOT contain `FamilyWall Kiosk` (old placeholder removed)
    - main.tsx contains `Toaster` import and render
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Complete family management UI wired together. FamilyList renders cards, all CRUD actions go through PIN authentication, success toasts appear, empty state displays when no members exist. ChangePinModal provides FAM-06 PIN change UI calling PUT /api/settings/pin. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification</name>
  <files>N/A</files>
  <action>Complete family management feature: list view with alphabetical sorting, add/edit via modal forms, delete with confirmation, PIN authentication (default PIN: 1234) protecting all parental actions, PIN change settings, touch-friendly 44px+ buttons, success toast notifications.</action>
  <verify>
    <automated>cd /Users/jassmith/Projects/gsdTest/client && npm run build 2>&1; BUILD=$?; echo "Build exit: $BUILD"; if [ $BUILD -eq 0 ]; then node -e "
const fs = require('fs');
let allOk = true;
function check(label, ok) { console.log((ok ? 'PASS' : 'FAIL') + ': ' + label); if (!ok) allOk = false; }

// Verify all 6 components exist and have minimum substance
const components = ['FamilyList','FamilyCard','FamilyFormModal','PinModal','DeleteConfirmModal','ChangePinModal'];
for (const c of components) {
  const p = 'src/components/' + c + '.tsx';
  const exists = fs.existsSync(p);
  check(c + ' exists', exists);
  if (exists) {
    const lines = fs.readFileSync(p, 'utf8').split('\n').length;
    check(c + ' has substance (>' + (c === 'FamilyCard' || c === 'DeleteConfirmModal' ? '15' : '30') + ' lines)', lines > (c === 'FamilyCard' || c === 'DeleteConfirmModal' ? 15 : 30));
  }
}

// Verify wiring: App -> FamilyList -> hooks -> modals
const app = fs.readFileSync('src/App.tsx','utf8');
check('App imports FamilyList', app.includes('FamilyList'));
const list = fs.readFileSync('src/components/FamilyList.tsx','utf8');
check('FamilyList imports all modals', list.includes('PinModal') && list.includes('FamilyFormModal') && list.includes('DeleteConfirmModal') && list.includes('ChangePinModal'));
check('FamilyList imports both hooks', list.includes('useFamilyData') && list.includes('usePinAuth'));
const main = fs.readFileSync('src/main.tsx','utf8');
check('main.tsx has Toaster for notifications', main.includes('Toaster'));

process.exit(allOk ? 0 : 1);
"; else echo 'FAIL: Build failed'; exit 1; fi</automated>
    Manual verification steps:
    1. Start the backend: `cd server && node index.js`
    2. Start the frontend: `cd client && npm run dev`
    3. Open browser to the dev URL (likely http://localhost:5173)
    4. Verify empty state shows "No Family Members Yet" with "Add Family Member" button
    5. Tap "Add Family Member" - PIN modal should appear with numeric keypad
    6. Enter PIN 1234 (default) - form modal should appear
    7. Enter name "Charlie" and tap "Add Member" - toast "Family member added successfully" should appear
    8. Add "Alice" and "Bob" - list should show Alice, Bob, Charlie (alphabetical, per D-08)
    9. Tap Edit on Bob's card - PIN modal appears, enter 1234 - edit form shows pre-filled "Bob"
    10. Change to "Bobby" and save - toast confirms, list re-sorts
    11. Tap Delete on Alice - PIN modal, enter 1234, confirm deletion - toast "Family member deleted"
    12. Tap settings gear icon - Change PIN modal appears
    13. Enter current PIN "1234", new PIN "5678", confirm "5678" - toast "PIN changed successfully"
    14. Try adding a member with new PIN 5678 - should work
    15. Verify all buttons feel responsive (scale effect on tap)
    16. Try wrong PIN (e.g. 0000) - should show "Incorrect PIN. Please try again." and clear digits
  </verify>
  <done>Type "approved" or describe issues</done>
</task>

</tasks>

<verification>
- Frontend builds: `cd client && npm run build`
- Backend starts: `cd server && node index.js`
- Full flow: add -> edit -> delete family members with PIN auth
- All modals open/close correctly
- Toast notifications appear and auto-dismiss
- Empty state renders when no members exist
- PIN can be changed via settings gear icon (FAM-06)
</verification>

<success_criteria>
- Family member list displays alphabetically sorted
- Add, Edit, Delete all require PIN (1234 default) before proceeding
- Form validation shows error messages for empty/too-long names
- Delete shows confirmation dialog with member name
- PIN modal has 56px numeric keypad buttons in 3-column grid
- All interactive elements are 44px+ touch targets
- Success toasts appear after each operation
- Settings gear icon opens Change PIN modal (FAM-06)
- PIN change requires current PIN and validates new PIN (per D-02)
- TypeScript compiles without errors
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation-family-management/02-03-SUMMARY.md`
</output>
